@Library('jenkins-shared-library-demo@main')_
node {
    stage('Hello World') {
        demo_1 'Bob' // this is the syntax to call a shared library .call(), see more example from this doc: https://www.jenkins.io/doc/book/pipeline/shared-libraries/#defining-custom-steps
    }
}
